{
  "name": "@poneglyph/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "BUN_PORT=3001 bun run --watch src/index.ts",
    "start": "BUN_PORT=3001 bun run src/index.ts",
    "build": "echo 'No build step needed'"
  },
  "dependencies": {
    "@poneglyph/data": "workspace:*",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}